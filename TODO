Next week:
- Test runs on CGvsNI
- Understand the loss (supervised vs unsupervised, weights and all)

- Redo the downloader for CG images



FONCTIONALITÉ :
  Performances sur le train ?
  Wesh je peux pas évaluer ça
Normaliser le display (pas la loss calculée)


CGvsNI setup:
  clean folder: dataset files
  raw folder: name_labels at the root of the folder, and then all images in their own folder by dataset
