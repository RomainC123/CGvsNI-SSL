from utils import *
import matplotlib.pyplot as plt

# total_epochs = 200
#
# schedule = WeightSchedule(ramp_up_epochs=50, ramp_up_mult=5, ramp_down_epochs=20, ramp_down_mult=12.5, total_epochs=total_epochs)
#
# list_weights = []
#
# for i in range(total_epochs):
#     list_weights.append(schedule.step())
#
# plt.plot(list(range(total_epochs)), list_weights)
# plt.show()
